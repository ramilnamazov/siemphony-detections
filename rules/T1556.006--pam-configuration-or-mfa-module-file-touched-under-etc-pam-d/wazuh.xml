<group name="sigma,linux,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^PATH$</field>
    <field name="name" type="pcre2">(?i)/etc/pam\.d/</field>
    <description>PAM configuration or MFA module file touched under /etc/pam.d (1/2)</description>
    <mitre>
      <id>T1556.006</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^PATH$</field>
    <field name="name" type="pcre2">(?i)(/pam_google_authenticator\.so$|/pam_duo\.so$|/pam_oath\.so$|/pam_yubico\.so$|/pam_u2f\.so$|/pam_radius_auth\.so$)</field>
    <description>PAM configuration or MFA module file touched under /etc/pam.d (2/2)</description>
    <mitre>
      <id>T1556.006</id>
    </mitre>
  </rule>
</group>
