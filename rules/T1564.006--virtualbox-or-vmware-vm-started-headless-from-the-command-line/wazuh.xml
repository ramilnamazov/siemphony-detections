<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\VBoxManage\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(?=.*(?:startvm))(?=.*(?:headless)).*</field>
    <description>VirtualBox or VMware VM started headless from the command line (1/3)</description>
    <mitre>
      <id>T1564.006</id>
    </mitre>
  </rule>

  <rule id="100001" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\VBoxHeadless\.exe$</field>
    <description>VirtualBox or VMware VM started headless from the command line (2/3)</description>
    <mitre>
      <id>T1564.006</id>
    </mitre>
  </rule>

  <rule id="100002" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\vmrun\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)nogui</field>
    <description>VirtualBox or VMware VM started headless from the command line (3/3)</description>
    <mitre>
      <id>T1564.006</id>
    </mitre>
  </rule>
</group>
