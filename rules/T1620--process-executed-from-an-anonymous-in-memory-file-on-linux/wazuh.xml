<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)memfd:</field>
    <field name="Image" negate="yes" type="pcre2">(?i)runc_cloned:/proc/self/exe</field>
    <description>Process executed from an anonymous in-memory file on Linux</description>
    <mitre>
      <id>T1620</id>
    </mitre>
  </rule>
</group>
