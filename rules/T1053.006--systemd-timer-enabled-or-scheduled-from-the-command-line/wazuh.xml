<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)/systemctl$</field>
    <field name="CommandLine" type="pcre2">(?i)\.timer</field>
    <description>Systemd timer enabled or scheduled from the command line (1/2)</description>
    <mitre>
      <id>T1053.006</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)/systemd-run$</field>
    <field name="CommandLine" type="pcre2">(?i)(--on-active|--on-boot|--on-startup|--on-unit-active|--on-unit-inactive|--on-calendar|--timer-property)</field>
    <description>Systemd timer enabled or scheduled from the command line (2/2)</description>
    <mitre>
      <id>T1053.006</id>
    </mitre>
  </rule>
</group>
