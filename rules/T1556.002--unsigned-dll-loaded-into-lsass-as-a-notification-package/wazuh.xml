<group name="sigma,windows,image_load,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\lsass\.exe$</field>
    <field name="SignatureStatus" negate="yes" type="pcre2">(?i)^Valid$</field>
    <description>Unsigned DLL loaded into LSASS as a notification package</description>
    <mitre>
      <id>T1556.002</id>
    </mitre>
  </rule>
</group>
