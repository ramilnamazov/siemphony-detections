DeviceProcessEvents
| where (ProcessCommandLine contains "driverquery" or (ProcessCommandLine contains "query" and ProcessCommandLine contains "CurrentControlSet\\Services") or ProcessCommandLine contains "CurrentControlSet\\HardwareProfiles")
