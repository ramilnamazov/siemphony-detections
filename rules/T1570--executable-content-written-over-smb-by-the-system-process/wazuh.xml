<group name="sigma,windows,file_event,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)^System$</field>
    <field name="TargetFilename" type="pcre2">(?i)(\.exe$|\.dll$|\.sys$|\.ps1$|\.bat$|\.cmd$|\.vbs$|\.hta$|\.scr$)</field>
    <description>Executable content written over SMB by the System process</description>
    <mitre>
      <id>T1570</id>
    </mitre>
  </rule>
</group>
