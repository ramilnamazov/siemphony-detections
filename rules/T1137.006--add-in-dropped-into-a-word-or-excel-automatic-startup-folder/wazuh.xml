<group name="sigma,windows,file_event,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="TargetFilename" type="pcre2">(?i)\\Microsoft\\Word\\STARTUP\\</field>
    <field name="TargetFilename" type="pcre2">(?i)(\.wll$|\.dotm$|\.dot$)</field>
    <field name="TargetFilename" negate="yes" type="pcre2">(?i)\\~\$</field>
    <field name="TargetFilename" negate="yes" type="pcre2">(?i)\\PERSONAL\.XLSB$</field>
    <description>Add-in dropped into a Word or Excel automatic startup folder (1/2)</description>
    <mitre>
      <id>T1137.006</id>
    </mitre>
  </rule>

  <rule id="100001" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="TargetFilename" type="pcre2">(?i)\\XLSTART\\</field>
    <field name="TargetFilename" type="pcre2">(?i)(\.xll$|\.xlam$|\.xla$|\.xlsm$|\.xlsb$|\.xls$|\.xltm$)</field>
    <field name="TargetFilename" negate="yes" type="pcre2">(?i)\\~\$</field>
    <field name="TargetFilename" negate="yes" type="pcre2">(?i)\\PERSONAL\.XLSB$</field>
    <description>Add-in dropped into a Word or Excel automatic startup folder (2/2)</description>
    <mitre>
      <id>T1137.006</id>
    </mitre>
  </rule>
</group>
