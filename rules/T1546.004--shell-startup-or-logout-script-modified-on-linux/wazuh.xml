<group name="sigma,linux,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^PATH$</field>
    <field name="name" type="pcre2">(?i)(\.bashrc$|\.bash_profile$|\.bash_login$|\.bash_logout$|\.bash_aliases$|\.profile$|\.zshrc$|\.zshenv$|\.zprofile$|\.zlogin$|\.zlogout$|/etc/profile$|/etc/bash\.bashrc$|/etc/bashrc$|/etc/zsh/zshrc$|/etc/zsh/zshenv$|/etc/zsh/zprofile$|/etc/zsh/zlogin$|/etc/zsh/zlogout$|/etc/zshrc$|/etc/zshenv$|/etc/zprofile$|/etc/zlogin$|/etc/zlogout$)</field>
    <description>Shell startup or logout script modified on Linux (1/2)</description>
    <mitre>
      <id>T1546.004</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^PATH$</field>
    <field name="name" type="pcre2">(?i)/etc/profile\.d/</field>
    <description>Shell startup or logout script modified on Linux (2/2)</description>
    <mitre>
      <id>T1546.004</id>
    </mitre>
  </rule>
</group>
