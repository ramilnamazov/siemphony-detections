<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 4 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(Win32_Product|Win32_InstalledWin32Program|Win32_InstalledStoreProgram|Win32_SoftwareElement|Win32_SoftwareFeature)</field>
    <description>Installed software inventory enumerated from the command line (1/4)</description>
    <mitre>
      <id>T1518</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)\\CurrentVersion\\Uninstall</field>
    <description>Installed software inventory enumerated from the command line (2/4)</description>
    <mitre>
      <id>T1518</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(Get-Package|Get-AppxPackage|Get-AppvClientPackage|Get-WindowsCapability|Get-InstalledModule)</field>
    <description>Installed software inventory enumerated from the command line (3/4)</description>
    <mitre>
      <id>T1518</id>
    </mitre>
  </rule>

  <rule id="100003" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\wmic\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i) product</field>
    <description>Installed software inventory enumerated from the command line (4/4)</description>
    <mitre>
      <id>T1518</id>
    </mitre>
  </rule>
</group>
