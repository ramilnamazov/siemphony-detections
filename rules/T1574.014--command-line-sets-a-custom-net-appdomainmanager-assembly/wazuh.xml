<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(APPDOMAIN_MANAGER_ASM|APPDOMAIN_MANAGER_TYPE|appDomainManagerAssembly|appDomainManagerType)</field>
    <description>Command line sets a custom .NET AppDomainManager assembly</description>
    <mitre>
      <id>T1574.014</id>
    </mitre>
  </rule>
</group>
