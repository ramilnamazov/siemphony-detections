<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(proxychains|3proxy|honeygain|earnapp|traffmonetizer|peer2profit|packetstream|packetshare|repocket|proxyrack|pawns-cli|iproyal|bitping|nodepay|getgrass|earnfm|mysterium|proxylite|9hits|speedshare|luminati|brightdata)</field>
    <description>Bandwidth monetisation client executed on a Linux host (1/2)</description>
    <mitre>
      <id>T1496.002</id>
    </mitre>
  </rule>

  <rule id="100001" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(proxychains|3proxy|honeygain|earnapp|traffmonetizer|peer2profit|packetstream|packetshare|repocket|proxyrack|pawns-cli|iproyal|bitping|nodepay|getgrass|earnfm|mysterium|proxylite|9hits|speedshare|luminati|brightdata)</field>
    <description>Bandwidth monetisation client executed on a Linux host (2/2)</description>
    <mitre>
      <id>T1496.002</id>
    </mitre>
  </rule>
</group>
