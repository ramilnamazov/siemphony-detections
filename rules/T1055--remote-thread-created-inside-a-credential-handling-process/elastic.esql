FROM logs-*
| WHERE (TO_LOWER(winlog.event_data.TargetImage) LIKE "*\\\\winlogon.exe" OR TO_LOWER(winlog.event_data.TargetImage) LIKE "*\\\\explorer.exe" OR TO_LOWER(winlog.event_data.TargetImage) LIKE "*\\\\logonui.exe" OR TO_LOWER(winlog.event_data.TargetImage) LIKE "*\\\\credentialuibroker.exe" OR TO_LOWER(winlog.event_data.TargetImage) LIKE "*\\\\chrome.exe" OR TO_LOWER(winlog.event_data.TargetImage) LIKE "*\\\\msedge.exe" OR TO_LOWER(winlog.event_data.TargetImage) LIKE "*\\\\firefox.exe")
