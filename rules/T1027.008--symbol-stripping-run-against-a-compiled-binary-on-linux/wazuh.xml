<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/strip$|-strip$|/sstrip$)</field>
    <description>Symbol stripping run against a compiled binary on Linux (1/3)</description>
    <mitre>
      <id>T1027.008</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/objcopy$|-objcopy$)</field>
    <field name="CommandLine" type="pcre2">(?i)(--strip-all|--strip-debug|--strip-unneeded|--strip-symbol|--discard-all)</field>
    <description>Symbol stripping run against a compiled binary on Linux (2/3)</description>
    <mitre>
      <id>T1027.008</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)/go$</field>
    <field name="CommandLine" type="pcre2">(?i)-ldflags</field>
    <field name="CommandLine" type="pcre2">(?i)(-s -w|-w -s)</field>
    <description>Symbol stripping run against a compiled binary on Linux (3/3)</description>
    <mitre>
      <id>T1027.008</id>
    </mitre>
  </rule>
</group>
