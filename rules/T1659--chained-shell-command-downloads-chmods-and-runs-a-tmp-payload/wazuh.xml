<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/sh$|/bash$|/dash$)</field>
    <field name="CommandLine" type="pcre2">(?i)(curl |wget )</field>
    <field name="CommandLine" type="pcre2">(?i)(/tmp/|/var/tmp/)</field>
    <field name="CommandLine" type="pcre2">(?i)(chmod \+x|chmod 755|chmod 777)</field>
    <description>Chained shell command downloads, chmods and runs a /tmp payload</description>
    <mitre>
      <id>T1659</id>
    </mitre>
  </rule>
</group>
