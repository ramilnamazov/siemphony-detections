<group name="sigma,linux,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^PATH$</field>
    <field name="name" type="pcre2">(?i)/tmp/\.[^/]*lock[^/]*$</field>
    <description>Predictable lock file created under tmp or run as an execution guardrail (1/3)</description>
    <mitre>
      <id>T1480.002</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^PATH$</field>
    <field name="name" type="pcre2">(?i)/var/(run|lock)/[^/]*lock[^/]*$</field>
    <description>Predictable lock file created under tmp or run as an execution guardrail (2/3)</description>
    <mitre>
      <id>T1480.002</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="key" type="pcre2">(?i)^mutex_lock$</field>
    <description>Predictable lock file created under tmp or run as an execution guardrail (3/3)</description>
    <mitre>
      <id>T1480.002</id>
    </mitre>
  </rule>
</group>
