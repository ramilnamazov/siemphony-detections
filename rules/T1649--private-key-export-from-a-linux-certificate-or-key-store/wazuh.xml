<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)/openssl$</field>
    <field name="CommandLine" type="pcre2">(?i)(pkcs12|pkcs8)</field>
    <description>Private key export from a Linux certificate or key store (1/3)</description>
    <mitre>
      <id>T1649</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/pk12util$|/certutil$)</field>
    <field name="CommandLine" type="pcre2">(?i)(/nssdb|/\.pki)</field>
    <description>Private key export from a Linux certificate or key store (2/3)</description>
    <mitre>
      <id>T1649</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)/keytool$</field>
    <field name="CommandLine" type="pcre2">(?i)(-importkeystore|-exportcert)</field>
    <description>Private key export from a Linux certificate or key store (3/3)</description>
    <mitre>
      <id>T1649</id>
    </mitre>
  </rule>
</group>
