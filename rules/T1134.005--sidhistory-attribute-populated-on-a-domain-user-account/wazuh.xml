<group name="sigma,windows,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="EventID" type="pcre2">(?i)^5136$</field>
    <field name="ObjectClass" type="pcre2">(?i)^user$</field>
    <field name="AttributeLDAPDisplayName" type="pcre2">(?i)^sIDHistory$</field>
    <description>sIDHistory attribute populated on a domain user account (1/3)</description>
    <mitre>
      <id>T1134.005</id>
    </mitre>
  </rule>

  <rule id="100001" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="EventID" type="pcre2">(?i)^4720$</field>
    <field name="SidHistory" type="pcre2">(?i)S-1-5-21-</field>
    <description>sIDHistory attribute populated on a domain user account (2/3)</description>
    <mitre>
      <id>T1134.005</id>
    </mitre>
  </rule>

  <rule id="100002" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="EventID" type="pcre2">(?i)^4738$</field>
    <field name="SidHistory" type="pcre2">(?i)S-1-5-21-</field>
    <description>sIDHistory attribute populated on a domain user account (3/3)</description>
    <mitre>
      <id>T1134.005</id>
    </mitre>
  </rule>
</group>
