<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/timedatectl$|/localectl$|/locale$)</field>
    <description>Shell tools query the host timezone and locale settings (1/2)</description>
    <mitre>
      <id>T1614</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(/etc/timezone|/etc/localtime|/usr/share/zoneinfo)</field>
    <description>Shell tools query the host timezone and locale settings (2/2)</description>
    <mitre>
      <id>T1614</id>
    </mitre>
  </rule>
</group>
