<group name="sigma,windows,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="EventID" type="pcre2">(?i)^4104$</field>
    <field name="ScriptBlockText" type="pcre2">(?i)(Copy-Item|copy-item|Robocopy|robocopy)</field>
    <field name="ScriptBlockText" type="pcre2">(?i)\\\\</field>
    <description>PowerShell script block copies a file from a UNC path</description>
    <mitre>
      <id>T1074.002</id>
    </mitre>
  </rule>
</group>
