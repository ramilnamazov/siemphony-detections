<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\ssh\.exe$|\\plink\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i) -[LRD][ ]?[0-9]</field>
    <description>Port forwarding flags on ssh, plink or netsh portproxy (1/2)</description>
    <mitre>
      <id>T1572</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\netsh\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(?=.*(?:portproxy))(?=.*(?:connectaddress)).*</field>
    <description>Port forwarding flags on ssh, plink or netsh portproxy (2/2)</description>
    <mitre>
      <id>T1572</id>
    </mitre>
  </rule>
</group>
