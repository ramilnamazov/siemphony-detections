<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 5 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\whoami\.exe$</field>
    <description>Logged-on user identity queried by a built-in enumeration tool (1/5)</description>
    <mitre>
      <id>T1033</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\quser\.exe$|\\qwinsta\.exe$)</field>
    <description>Logged-on user identity queried by a built-in enumeration tool (2/5)</description>
    <mitre>
      <id>T1033</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\query\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)( user| session)</field>
    <description>Logged-on user identity queried by a built-in enumeration tool (3/5)</description>
    <mitre>
      <id>T1033</id>
    </mitre>
  </rule>

  <rule id="100003" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\net\.exe$|\\net1\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i) user</field>
    <description>Logged-on user identity queried by a built-in enumeration tool (4/5)</description>
    <mitre>
      <id>T1033</id>
    </mitre>
  </rule>

  <rule id="100004" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(WindowsIdentity\]::GetCurrent|Environment\]::UserName|\$env:USERNAME|Win32_LoggedOnUser)</field>
    <description>Logged-on user identity queried by a built-in enumeration tool (5/5)</description>
    <mitre>
      <id>T1033</id>
    </mitre>
  </rule>
</group>
