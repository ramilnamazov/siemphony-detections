FROM logs-*
| WHERE (event.code == 5145 AND (TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.pst" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.ost" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.nst" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.olm"))
