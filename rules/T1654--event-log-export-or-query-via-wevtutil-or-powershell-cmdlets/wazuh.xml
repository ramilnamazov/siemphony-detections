<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="NewProcessName" type="pcre2">(?i)\\wevtutil\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(epl|qe)</field>
    <description>Event log export or query via wevtutil or PowerShell cmdlets (1/2)</description>
    <mitre>
      <id>T1654</id>
    </mitre>
  </rule>

  <rule id="100001" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="NewProcessName" type="pcre2">(?i)(\\powershell\.exe$|\\pwsh\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i)(Get-WinEvent|Get-EventLog)</field>
    <description>Event log export or query via wevtutil or PowerShell cmdlets (2/2)</description>
    <mitre>
      <id>T1654</id>
    </mitre>
  </rule>
</group>
