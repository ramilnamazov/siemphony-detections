<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\net\.exe$|\\net1\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i) accounts</field>
    <description>Local or domain password policy queried from the command line (1/3)</description>
    <mitre>
      <id>T1201</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\secedit\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)/export</field>
    <description>Local or domain password policy queried from the command line (2/3)</description>
    <mitre>
      <id>T1201</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\powershell\.exe$|\\pwsh\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i)(Get-ADDefaultDomainPasswordPolicy|Get-ADFineGrainedPasswordPolicy)</field>
    <description>Local or domain password policy queried from the command line (3/3)</description>
    <mitre>
      <id>T1201</id>
    </mitre>
  </rule>
</group>
