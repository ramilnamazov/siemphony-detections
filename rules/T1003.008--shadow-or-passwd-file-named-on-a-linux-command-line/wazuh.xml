<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)/etc/shadow</field>
    <description>Shadow or passwd file named on a Linux command line (1/3)</description>
    <mitre>
      <id>T1003.008</id>
    </mitre>
  </rule>

  <rule id="100001" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)/unshadow$</field>
    <description>Shadow or passwd file named on a Linux command line (2/3)</description>
    <mitre>
      <id>T1003.008</id>
    </mitre>
  </rule>

  <rule id="100002" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)/etc/passwd</field>
    <field name="Image" type="pcre2">(?i)(/cp$|/tar$|/base64$|/xxd$|/curl$|/scp$|/nc$|/nc\.openbsd$|/nc\.traditional$|/netcat$|/ncat$)</field>
    <description>Shadow or passwd file named on a Linux command line (3/3)</description>
    <mitre>
      <id>T1003.008</id>
    </mitre>
  </rule>
</group>
