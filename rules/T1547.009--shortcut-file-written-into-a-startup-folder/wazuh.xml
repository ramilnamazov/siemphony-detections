<group name="sigma,windows,file_event,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="TargetFilename" type="pcre2">(?i)\.lnk$</field>
    <field name="TargetFilename" type="pcre2">(?i)\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\\</field>
    <description>Shortcut file written into a Startup folder</description>
    <mitre>
      <id>T1547.009</id>
    </mitre>
  </rule>
</group>
