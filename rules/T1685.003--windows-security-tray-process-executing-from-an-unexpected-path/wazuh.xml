<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\SecurityHealthSystray\.exe$|\\SecurityHealthService\.exe$)</field>
    <field name="Image" negate="yes" type="pcre2">(?i)(^C:\\Windows\\System32\\|^C:\\Windows\\SysWOW64\\)</field>
    <description>Windows Security tray process executing from an unexpected path</description>
    <mitre>
      <id>T1685.003</id>
    </mitre>
  </rule>
</group>
