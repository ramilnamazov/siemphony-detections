FROM logs-*
| WHERE ((event.code == 4768 AND TO_LOWER(winlog.event_data.PreAuthType) == "0" AND TO_LOWER(winlog.event_data.Status) == "0x0") AND NOT (TO_LOWER(winlog.event_data.TargetUserName) LIKE "*$"))
