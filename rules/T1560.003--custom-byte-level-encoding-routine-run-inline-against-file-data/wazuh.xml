<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\powershell\.exe$|\\pwsh\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i)(-bxor|-bnot|-band|-shl|-shr)</field>
    <field name="CommandLine" type="pcre2">(?i)(ReadAllBytes|WriteAllBytes|FileStream|OpenRead|OpenWrite|Get-Content|Set-Content|-AsByteStream|-Encoding Byte)</field>
    <description>Custom byte-level encoding routine run inline against file data</description>
    <mitre>
      <id>T1560.003</id>
    </mitre>
  </rule>
</group>
