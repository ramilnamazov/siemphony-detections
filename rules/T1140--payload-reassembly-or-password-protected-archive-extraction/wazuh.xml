<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\cmd\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)copy /b</field>
    <description>Payload reassembly or password-protected archive extraction (1/3)</description>
    <mitre>
      <id>T1140</id>
    </mitre>
  </rule>

  <rule id="100001" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\expand\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(-f:|/f:|–f:|—f:|―f:)</field>
    <description>Payload reassembly or password-protected archive extraction (2/3)</description>
    <mitre>
      <id>T1140</id>
    </mitre>
  </rule>

  <rule id="100002" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\7z\.exe$|\\7za\.exe$|\\rar\.exe$|\\unrar\.exe$|\\winrar\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i) -p</field>
    <description>Payload reassembly or password-protected archive extraction (3/3)</description>
    <mitre>
      <id>T1140</id>
    </mitre>
  </rule>
</group>
