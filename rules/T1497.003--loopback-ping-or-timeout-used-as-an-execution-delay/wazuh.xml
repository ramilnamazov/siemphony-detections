<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\ping\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(127\.0\.0\.1|localhost|::1)</field>
    <description>Loopback ping or timeout used as an execution delay (1/2)</description>
    <mitre>
      <id>T1497.003</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\timeout\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(-nobreak|/nobreak|–nobreak|—nobreak|―nobreak)</field>
    <description>Loopback ping or timeout used as an execution delay (2/2)</description>
    <mitre>
      <id>T1497.003</id>
    </mitre>
  </rule>
</group>
