<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\certutil\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(-decode|/decode|–decode|—decode|―decode|-decodehex|/decodehex|–decodehex|—decodehex|―decodehex)</field>
    <description>Encoded payload file decoded by certutil or PowerShell (1/2)</description>
    <mitre>
      <id>T1027.013</id>
    </mitre>
  </rule>

  <rule id="100001" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\powershell\.exe$|\\pwsh\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i)(FromBase64String|FromBase64Transform|CreateDecryptor|GzipStream)</field>
    <field name="CommandLine" type="pcre2">(?i)(Get-Content|ReadAllBytes|ReadAllText|OpenRead)</field>
    <description>Encoded payload file decoded by certutil or PowerShell (2/2)</description>
    <mitre>
      <id>T1027.013</id>
    </mitre>
  </rule>
</group>
