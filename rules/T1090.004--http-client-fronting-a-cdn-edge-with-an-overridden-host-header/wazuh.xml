<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/curl$|/wget$|/busybox$)</field>
    <field name="CommandLine" type="pcre2">(?i)(-H Host:|-HHost:|--header=Host:)</field>
    <field name="CommandLine" type="pcre2">(?i)https://</field>
    <field name="CommandLine" type="pcre2">(?i)(cloudfront\.net|azureedge\.net|fastly\.net|akamaihd\.net)</field>
    <description>HTTP client fronting a CDN edge with an overridden Host header</description>
    <mitre>
      <id>T1090.004</id>
    </mitre>
  </rule>
</group>
