<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 5 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\netsh\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(state off|set opmode disable|advfirewall reset)</field>
    <description>Host firewall disabled or opened from a command line (1/5)</description>
    <mitre>
      <id>T1686</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\netsh\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(?=.*(?:firewall add rule))(?=.*(?:action=allow)).*</field>
    <description>Host firewall disabled or opened from a command line (2/5)</description>
    <mitre>
      <id>T1686</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(Set-NetFirewallProfile|New-NetFirewallRule|Disable-NetFirewallRule|Remove-NetFirewallRule)</field>
    <description>Host firewall disabled or opened from a command line (3/5)</description>
    <mitre>
      <id>T1686</id>
    </mitre>
  </rule>

  <rule id="100003" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\sc\.exe$|\\net\.exe$|\\net1\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i)mpssvc</field>
    <description>Host firewall disabled or opened from a command line (4/5)</description>
    <mitre>
      <id>T1686</id>
    </mitre>
  </rule>

  <rule id="100004" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(?=.*(?:SharedAccess\\Parameters\\FirewallPolicy))(?=.*(?:EnableFirewall)).*</field>
    <description>Host firewall disabled or opened from a command line (5/5)</description>
    <mitre>
      <id>T1686</id>
    </mitre>
  </rule>
</group>
