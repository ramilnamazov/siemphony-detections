<group name="sigma,windows,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="EventID" type="pcre2">(?i)^4104$</field>
    <field name="ScriptBlockText" type="pcre2">(?i)(Invoke-GlobalMailSearch|Invoke-SelfSearch|Invoke-OpenInboxFinder|Get-GlobalAddressList|Search-Mailbox|New-MailboxSearch|New-MailboxExportRequest|New-ComplianceSearchAction|Get-MgUserMessage|Get-MgUserMailFolderMessage|Microsoft\.Exchange\.WebServices|AADInternals)</field>
    <description>Mailbox search and export tooling in a script block</description>
    <mitre>
      <id>T1114.002</id>
    </mitre>
  </rule>
</group>
