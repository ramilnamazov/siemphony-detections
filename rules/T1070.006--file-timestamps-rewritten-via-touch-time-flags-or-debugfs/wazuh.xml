<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)/touch$</field>
    <field name="CommandLine" type="pcre2">(?i)( -t | -d | -r | --date| --reference| --time=| -at | -mt | -amt | -ar | -mr | -amr | -ad | -md | -amd )</field>
    <description>File timestamps rewritten via touch time flags or debugfs (1/2)</description>
    <mitre>
      <id>T1070.006</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)/debugfs$</field>
    <field name="CommandLine" type="pcre2">(?i)set_inode_field</field>
    <description>File timestamps rewritten via touch time flags or debugfs (2/2)</description>
    <mitre>
      <id>T1070.006</id>
    </mitre>
  </rule>
</group>
