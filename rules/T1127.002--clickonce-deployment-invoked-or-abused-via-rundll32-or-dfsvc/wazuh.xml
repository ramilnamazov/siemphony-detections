<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\rundll32\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)dfshim</field>
    <field name="CommandLine" type="pcre2">(?i)ShOpenVerbApplication</field>
    <description>ClickOnce deployment invoked or abused via rundll32 or dfsvc (1/2)</description>
    <mitre>
      <id>T1127.002</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="ParentImage" type="pcre2">(?i)\\dfsvc\.exe$</field>
    <field name="Image" type="pcre2">(?i)(\\cmd\.exe$|\\powershell\.exe$|\\pwsh\.exe$|\\wscript\.exe$|\\cscript\.exe$|\\mshta\.exe$|\\rundll32\.exe$|\\regsvr32\.exe$|\\certutil\.exe$|\\bitsadmin\.exe$)</field>
    <description>ClickOnce deployment invoked or abused via rundll32 or dfsvc (2/2)</description>
    <mitre>
      <id>T1127.002</id>
    </mitre>
  </rule>
</group>
