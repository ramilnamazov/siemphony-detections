<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\cmd\.exe$|\\powershell\.exe$|\\pwsh\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i)((?:^|[\s&amp;|(&quot;])dir(?:\s+[^&amp;|;]+)?\s+/s(?:[\s&quot;]|$)|(?:^|[\s&amp;|(&quot;])tree(?:\.com)?(?:\s+[^&amp;|;]+)?\s+/f(?:[\s&quot;]|$)|forfiles(?:\.exe)?(?:\s+[^&amp;|;]+)?\s+/s(?:[\s&quot;]|$)|(?:^|[\s;|({&quot;])(?:get-childitem|gci|ls|dir)(?:\s+[^;|]+)?\s+-(?:r|re|rec|recu|recur|recurs|recurse)(?:[\s&quot;]|$))</field>
    <description>Recursive directory listing issued from a Windows shell</description>
    <mitre>
      <id>T1083</id>
    </mitre>
  </rule>
</group>
