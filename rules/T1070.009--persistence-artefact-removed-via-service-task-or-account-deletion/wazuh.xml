<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 5 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\sc\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i) delete </field>
    <description>Persistence artefact removed via service, task or account deletion (1/5)</description>
    <mitre>
      <id>T1070.009</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\schtasks\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(-delete|/delete|–delete|—delete|―delete)</field>
    <description>Persistence artefact removed via service, task or account deletion (2/5)</description>
    <mitre>
      <id>T1070.009</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\reg\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i) delete </field>
    <field name="CommandLine" type="pcre2">(?i)(\\CurrentVersion\\Run|\\Image File Execution Options\\|\\CurrentControlSet\\Services\\)</field>
    <description>Persistence artefact removed via service, task or account deletion (3/5)</description>
    <mitre>
      <id>T1070.009</id>
    </mitre>
  </rule>

  <rule id="100003" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\net\.exe$|\\net1\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i)(?=.*(?: user ))(?=.*(?:/delete)).*</field>
    <description>Persistence artefact removed via service, task or account deletion (4/5)</description>
    <mitre>
      <id>T1070.009</id>
    </mitre>
  </rule>

  <rule id="100004" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(Unregister-ScheduledTask|Remove-LocalUser|Remove-Service)</field>
    <description>Persistence artefact removed via service, task or account deletion (5/5)</description>
    <mitre>
      <id>T1070.009</id>
    </mitre>
  </rule>
</group>
