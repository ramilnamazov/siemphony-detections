<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/t50$|/mz$|/mausezahn$|/slowhttptest$|/hyenae$)</field>
    <description>Packet flood tool launched with a continuous-send argument (1/3)</description>
    <mitre>
      <id>T1498.001</id>
    </mitre>
  </rule>

  <rule id="100001" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/hping$|/hping2$|/hping3$|/nping$)</field>
    <field name="CommandLine" type="pcre2">(?i)(--flood|--rand-source|--faster|--rate | -i u)</field>
    <description>Packet flood tool launched with a continuous-send argument (2/3)</description>
    <mitre>
      <id>T1498.001</id>
    </mitre>
  </rule>

  <rule id="100002" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/ping$|/ping6$)</field>
    <field name="CommandLine" type="pcre2">(?i) -f </field>
    <description>Packet flood tool launched with a continuous-send argument (3/3)</description>
    <mitre>
      <id>T1498.001</id>
    </mitre>
  </rule>
</group>
