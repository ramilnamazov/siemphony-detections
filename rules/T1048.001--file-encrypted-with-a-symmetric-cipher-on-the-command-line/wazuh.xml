<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)/openssl$</field>
    <field name="CommandLine" type="pcre2">(?i) enc</field>
    <description>File encrypted with a symmetric cipher on the command line (1/3)</description>
    <mitre>
      <id>T1048.001</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/gpg$|/gpg1$|/gpg2$)</field>
    <field name="CommandLine" type="pcre2">(?i)(--symmetric|--cipher-algo| -c )</field>
    <description>File encrypted with a symmetric cipher on the command line (2/3)</description>
    <mitre>
      <id>T1048.001</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(aes-128-cbc|aes-192-cbc|aes-256-cbc|aes-256-ctr|aes-256-gcm|-aes128|-aes192|-aes256|chacha20|-des3|-rc4| rc4 )</field>
    <description>File encrypted with a symmetric cipher on the command line (3/3)</description>
    <mitre>
      <id>T1048.001</id>
    </mitre>
  </rule>
</group>
