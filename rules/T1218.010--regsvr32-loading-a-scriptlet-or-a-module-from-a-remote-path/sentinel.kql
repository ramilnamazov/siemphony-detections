DeviceProcessEvents
| where (FolderPath endswith "\\regsvr32.exe" and ((ProcessCommandLine contains "http://" or ProcessCommandLine contains "https://" or ProcessCommandLine contains "ftp://" or ProcessCommandLine contains "\\\\") or (ProcessCommandLine contains "scrobj.dll" or ProcessCommandLine contains ".sct")))
