<group name="sigma,windows,file_delete,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="TargetFilename" type="pcre2">(?i)(\\AppData\\Local\\Temp\\|\\AppData\\Roaming\\|\\Windows\\Temp\\|\\Users\\Public\\|\\ProgramData\\|\\PerfLogs\\)</field>
    <field name="TargetFilename" type="pcre2">(?i)(\.exe$|\.dll$|\.ps1$|\.bat$|\.cmd$|\.vbs$|\.js$|\.hta$|\.scr$)</field>
    <description>Executable artefact deleted from a staging directory (1/2)</description>
    <mitre>
      <id>T1070.004</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\sdelete\.exe$|\\sdelete64\.exe$)</field>
    <description>Executable artefact deleted from a staging directory (2/2)</description>
    <mitre>
      <id>T1070.004</id>
    </mitre>
  </rule>
</group>
