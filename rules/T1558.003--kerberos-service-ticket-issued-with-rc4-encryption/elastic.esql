FROM logs-*
| WHERE ((event.code == 4769 AND TO_LOWER(TicketEncryptionType) == "0x17" AND TO_LOWER(Status) == "0x0") AND NOT ((TO_LOWER(service.name) LIKE "*$" OR TO_LOWER(service.name) LIKE "*krbtgt")))
