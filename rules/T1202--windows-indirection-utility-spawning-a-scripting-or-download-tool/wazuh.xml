<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="ParentImage" type="pcre2">(?i)(\\forfiles\.exe$|\\pcalua\.exe$|\\scriptrunner\.exe$)</field>
    <field name="Image" type="pcre2">(?i)(\\powershell\.exe$|\\pwsh\.exe$|\\cmd\.exe$|\\wscript\.exe$|\\cscript\.exe$|\\mshta\.exe$|\\rundll32\.exe$|\\regsvr32\.exe$|\\msiexec\.exe$|\\curl\.exe$|\\bitsadmin\.exe$|\\certutil\.exe$)</field>
    <description>Windows indirection utility spawning a scripting or download tool (1/2)</description>
    <mitre>
      <id>T1202</id>
    </mitre>
  </rule>

  <rule id="100001" level="7">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\ssh\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(ProxyCommand|LocalCommand)</field>
    <description>Windows indirection utility spawning a scripting or download tool (2/2)</description>
    <mitre>
      <id>T1202</id>
    </mitre>
  </rule>
</group>
