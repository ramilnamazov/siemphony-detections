FROM logs-*
| WHERE ((TO_LOWER(winlog.provider_name) == "service control manager" AND event.code == 7045) AND (TO_LOWER(winlog.event_data.ImagePath) LIKE "*tscon*" OR TO_LOWER(winlog.event_data.ImagePath) LIKE "*rdp-tcp#*"))
