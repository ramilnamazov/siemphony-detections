<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="ParentImage" type="pcre2">(?i)(/sshd$|/sshd-session$)</field>
    <field name="Image" type="pcre2">(?i)(/bash$|/sh$|/dash$|/zsh$)</field>
    <field name="CommandLine" type="pcre2">(?i)(base64 -d|base64 --decode|/dev/tcp/|nc -e|ncat -e|socat exec|mkfifo|0&gt;&amp;1)</field>
    <description>Suspicious one-line command executed as a direct child of sshd</description>
    <mitre>
      <id>T1021.004</id>
    </mitre>
  </rule>
</group>
