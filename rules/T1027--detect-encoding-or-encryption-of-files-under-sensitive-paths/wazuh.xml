<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(openssl enc|gpg --symmetric|gpg -c |base64 |xxd -p)</field>
    <field name="CommandLine" type="pcre2">(?i)(/etc/|/home/|/root/|/srv/|/var/www/)</field>
    <field name="CommandLine" negate="yes" type="pcre2">(?i)( -d |--decode|--decrypt)</field>
    <description>Detect encoding or encryption of files under sensitive paths</description>
    <mitre>
      <id>T1027</id>
    </mitre>
  </rule>
</group>
