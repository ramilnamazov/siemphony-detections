<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="7">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/find$|/grep$)</field>
    <field name="CommandLine" type="pcre2">(?i)( /home| /root| /srv| /var/www| /opt| /mnt| /media| /export)</field>
    <field name="CommandLine" type="pcre2">(?i)(id_rsa|id_ed25519|id_ecdsa|authorized_keys|/\.ssh|/\.aws|/\.kube|\.git-credentials|\.netrc|\.pgpass|\.kdbx|\.ovpn|\.docx|\.xlsx|\.pdf|\.sql)</field>
    <description>Recursive search for sensitive files under a shared root</description>
    <mitre>
      <id>T1005</id>
    </mitre>
  </rule>
</group>
