<group name="sigma,linux,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 4 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^EXECVE$</field>
    <field name="a0" type="pcre2">(?i)(hping$|hping2$|hping3$|nping$)</field>
    <field name="a1" type="pcre2">(?i)(-S|--syn|-A|--ack|--flood)</field>
    <description>Packet generator invoked with SYN, ACK or flood arguments on Linux (1/4)</description>
    <mitre>
      <id>T1499.001</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^EXECVE$</field>
    <field name="a0" type="pcre2">(?i)(hping$|hping2$|hping3$|nping$)</field>
    <field name="a2" type="pcre2">(?i)(-S|--syn|-A|--ack|--flood)</field>
    <description>Packet generator invoked with SYN, ACK or flood arguments on Linux (2/4)</description>
    <mitre>
      <id>T1499.001</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^EXECVE$</field>
    <field name="a0" type="pcre2">(?i)(hping$|hping2$|hping3$|nping$)</field>
    <field name="a3" type="pcre2">(?i)(-S|--syn|-A|--ack|--flood)</field>
    <description>Packet generator invoked with SYN, ACK or flood arguments on Linux (3/4)</description>
    <mitre>
      <id>T1499.001</id>
    </mitre>
  </rule>

  <rule id="100003" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^EXECVE$</field>
    <field name="a0" type="pcre2">(?i)(hping$|hping2$|hping3$|nping$)</field>
    <field name="a4" type="pcre2">(?i)(-S|--syn|-A|--ack|--flood)</field>
    <description>Packet generator invoked with SYN, ACK or flood arguments on Linux (4/4)</description>
    <mitre>
      <id>T1499.001</id>
    </mitre>
  </rule>
</group>
