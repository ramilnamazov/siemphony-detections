<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\wevtutil\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)/e:false</field>
    <description>Audit policy or Windows Event Log channel disabled via CLI tooling (1/3)</description>
    <mitre>
      <id>T1685.001</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\auditpol\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)(/success:disable|/failure:disable)</field>
    <description>Audit policy or Windows Event Log channel disabled via CLI tooling (2/3)</description>
    <mitre>
      <id>T1685.001</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\sc\.exe$|\\net\.exe$|\\net1\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i)eventlog</field>
    <description>Audit policy or Windows Event Log channel disabled via CLI tooling (3/3)</description>
    <mitre>
      <id>T1685.001</id>
    </mitre>
  </rule>
</group>
