<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(\\net\.exe$|\\net1\.exe$)</field>
    <field name="CommandLine" type="pcre2">(?i)(?=.*(?: group ))(?=.*(?:/domain)).*</field>
    <description>Domain group enumeration via net group or the AD cmdlets (1/2)</description>
    <mitre>
      <id>T1069.002</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(Get-ADGroup|Get-ADPrincipalGroupMembership|Get-DomainGroup|Get-NetGroup|dsquery group|dsget group)</field>
    <description>Domain group enumeration via net group or the AD cmdlets (2/2)</description>
    <mitre>
      <id>T1069.002</id>
    </mitre>
  </rule>
</group>
