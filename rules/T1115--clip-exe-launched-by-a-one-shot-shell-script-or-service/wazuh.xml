<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 2 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\clip\.exe$</field>
    <field name="ParentImage" type="pcre2">(?i)(\\cmd\.exe$|\\powershell\.exe$|\\pwsh\.exe$)</field>
    <field name="ParentCommandLine" type="pcre2">(?i)(-c |/c |–c |—c |―c |-command|/command|–command|—command|―command|-encodedcommand|/encodedcommand|–encodedcommand|—encodedcommand|―encodedcommand|-enc |/enc |–enc |—enc |―enc )</field>
    <description>clip.exe launched by a one-shot shell, script or service (1/2)</description>
    <mitre>
      <id>T1115</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\clip\.exe$</field>
    <field name="ParentImage" negate="yes" type="pcre2">(?i)(\\explorer\.exe$|\\cmd\.exe$|\\powershell\.exe$|\\pwsh\.exe$)</field>
    <description>clip.exe launched by a one-shot shell, script or service (2/2)</description>
    <mitre>
      <id>T1115</id>
    </mitre>
  </rule>
</group>
