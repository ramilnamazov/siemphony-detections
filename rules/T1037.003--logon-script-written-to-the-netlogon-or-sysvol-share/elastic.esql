FROM logs-*
| WHERE (event.code == 5145 AND (TO_LOWER(winlog.event_data.ShareName) LIKE "*\\\\netlogon" OR TO_LOWER(winlog.event_data.ShareName) LIKE "*\\\\sysvol") AND (TO_LOWER(winlog.event_data.AccessList) LIKE "*%%4417*" OR TO_LOWER(winlog.event_data.AccessList) LIKE "*%%4418*" OR TO_LOWER(winlog.event_data.AccessList) LIKE "*writedata*" OR TO_LOWER(winlog.event_data.AccessList) LIKE "*appenddata*") AND (TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.bat" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.cmd" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.ps1" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.vbs" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.vbe" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.js" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.jse" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.wsf" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.hta" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.exe" OR TO_LOWER(winlog.event_data.RelativeTargetName) LIKE "*.dll"))
