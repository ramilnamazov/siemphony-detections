<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="ParentImage" type="pcre2">(?i)(/python3$|/python$)</field>
    <field name="ParentCommandLine" type="pcre2">(?i)(pip|poetry)</field>
    <field name="Image" type="pcre2">(?i)(/sh$|/bash$|/dash$|/python3$|/python$|/node$|/curl$|/wget$|/perl$)</field>
    <description>Package manager spawning a shell or interpreter on Linux (1/3)</description>
    <mitre>
      <id>T1204.005</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="ParentImage" type="pcre2">(?i)/node$</field>
    <field name="ParentCommandLine" type="pcre2">(?i)(npm|npx)</field>
    <field name="Image" type="pcre2">(?i)(/sh$|/bash$|/dash$|/python3$|/python$|/node$|/curl$|/wget$|/perl$)</field>
    <description>Package manager spawning a shell or interpreter on Linux (2/3)</description>
    <mitre>
      <id>T1204.005</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="ParentImage" type="pcre2">(?i)/ruby$</field>
    <field name="ParentCommandLine" type="pcre2">(?i)gem</field>
    <field name="Image" type="pcre2">(?i)(/sh$|/bash$|/dash$|/python3$|/python$|/node$|/curl$|/wget$|/perl$)</field>
    <description>Package manager spawning a shell or interpreter on Linux (3/3)</description>
    <mitre>
      <id>T1204.005</id>
    </mitre>
  </rule>
</group>
