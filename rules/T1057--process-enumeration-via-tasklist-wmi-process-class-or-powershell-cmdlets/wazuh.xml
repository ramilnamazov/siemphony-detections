<group name="sigma,windows,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\tasklist\.exe$</field>
    <description>Process enumeration via tasklist, WMI process class or PowerShell cmdlets (1/3)</description>
    <mitre>
      <id>T1057</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="CommandLine" type="pcre2">(?i)(Get-Process|Get-CimInstance -ClassName Win32_Process|Get-CimInstance Win32_Process|Get-WmiObject -Class Win32_Process|Get-WmiObject Win32_Process)</field>
    <description>Process enumeration via tasklist, WMI process class or PowerShell cmdlets (2/3)</description>
    <mitre>
      <id>T1057</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for windows so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)\\wmic\.exe$</field>
    <field name="CommandLine" type="pcre2">(?i)process</field>
    <description>Process enumeration via tasklist, WMI process class or PowerShell cmdlets (3/3)</description>
    <mitre>
      <id>T1057</id>
    </mitre>
  </rule>
</group>
