<group name="sigma,linux,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <!-- 3 rules: the Sigma condition ORs across different fields,
       which one rule cannot express. Any one matching is a hit. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^SYSCALL$</field>
    <field name="exe" type="pcre2">(?i)(/insmod$|/rmmod$|/modprobe$|/depmod$|/kmod$)</field>
    <description>Kernel module loaded or the module tree modified on Linux (1/3)</description>
    <mitre>
      <id>T1547.006</id>
    </mitre>
  </rule>

  <rule id="100001" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^PATH$</field>
    <field name="name" type="pcre2">(?i)(\.ko$|\.ko\.xz$|\.ko\.zst$|\.ko\.gz$)</field>
    <description>Kernel module loaded or the module tree modified on Linux (2/3)</description>
    <mitre>
      <id>T1547.006</id>
    </mitre>
  </rule>

  <rule id="100002" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="type" type="pcre2">(?i)^PATH$</field>
    <field name="name" type="pcre2">(?i)/lib/modules/</field>
    <description>Kernel module loaded or the module tree modified on Linux (3/3)</description>
    <mitre>
      <id>T1547.006</id>
    </mitre>
  </rule>
</group>
