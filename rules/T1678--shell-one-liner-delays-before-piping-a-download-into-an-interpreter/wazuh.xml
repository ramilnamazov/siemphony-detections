<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/sh$|/bash$|/dash$|/ash$)</field>
    <field name="CommandLine" type="pcre2">(?i)(sleep |ping )</field>
    <field name="CommandLine" type="pcre2">(?i)(curl|wget|\|sh|\| sh|\|bash|\| bash)</field>
    <description>Shell one-liner delays before piping a download into an interpreter</description>
    <mitre>
      <id>T1678</id>
    </mitre>
  </rule>
</group>
