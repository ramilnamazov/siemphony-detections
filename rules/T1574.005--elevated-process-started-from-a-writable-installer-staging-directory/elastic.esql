FROM logs-*
| WHERE ((TO_LOWER(IntegrityLevel) == "high" OR TO_LOWER(IntegrityLevel) == "system") AND (TO_LOWER(process.executable) LIKE "*\\\\appdata\\\\local\\\\temp\\\\*" OR TO_LOWER(process.executable) LIKE "*:\\\\windows\\\\temp\\\\*"))
