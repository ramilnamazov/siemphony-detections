FROM logs-*
| WHERE ((TO_LOWER(winlog.event_data.IntegrityLevel) == "high" OR TO_LOWER(winlog.event_data.IntegrityLevel) == "system") AND (TO_LOWER(process.executable) LIKE "*\\\\appdata\\\\local\\\\temp\\\\*" OR TO_LOWER(process.executable) LIKE "*:\\\\windows\\\\temp\\\\*"))
