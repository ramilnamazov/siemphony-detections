<group name="sigma,linux,process_creation,">
  <!-- Rule ids must be unique on your manager; 100000+ is the user range. -->
  <rule id="100000" level="5">
    <!-- Set <if_sid> to the decoder/base rule for linux so this only evaluates relevant events. -->
    <field name="Image" type="pcre2">(?i)(/docker$|/podman$|/nerdctl$)</field>
    <field name="CommandLine" type="pcre2">(?i)(--restart=always|--restart always|--restart=unless-stopped|--restart unless-stopped)</field>
    <description>Container started with a persistent restart policy</description>
    <mitre>
      <id>T1543.005</id>
    </mitre>
  </rule>
</group>
